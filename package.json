{
  "name": "ventotype",
  "version": "0.2.0",
  "description": "Background dictation app powered by any OpenAI-compatible Whisper endpoint.",
  "main": "out/main/index.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "dev": "npm run build:fn-hook && electron-vite dev",
    "build": "npm run build:fn-hook && electron-vite build",
    "build:fn-hook": "node scripts/build-fn-hook.mjs",
    "start": "electron-vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json",
    "pack:mac": "npm run build && electron-builder --mac --dir",
    "pack:win": "npm run build && electron-builder --win --dir",
    "dist:mac": "npm run build && electron-builder --mac",
    "dist:win": "npm run build && electron-builder --win"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "electron": "^43.2.0",
    "electron-builder": "^24.13.3",
    "electron-vite": "^2.2.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  },
  "build": {
    "appId": "com.ventotype.app",
    "productName": "VentoType",
    "icon": "resources/icon.png",
    "files": [
      "out/**/*",
      "resources/**/*",
      "!resources/fn-hook"
    ],
    "mac": {
      "category": "public.app-category.productivity",
      "extraResources": [
        {
          "from": "resources/fn-hook",
          "to": "fn-hook"
        }
      ],
      "extendInfo": {
        "NSMicrophoneUsageDescription": "VentoType needs microphone access to record dictation.",
        "LSUIElement": true
      }
    },
    "win": {
      "target": "nsis"
    }
  }
}
